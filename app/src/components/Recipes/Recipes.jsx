const Recipes = () => {
  /*
  This component will be passed in a search query
  On render, it will:
    1. Fetch recipes based on the query
    2. Render the recipes, or an error if something went wrong.
  */
  return <div id="recipes"></div>;
};

export default Recipes;
